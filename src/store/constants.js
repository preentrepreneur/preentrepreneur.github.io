const namespace = "LOGIN_"

export default {
    SET_USER_TYPE: `${namespace}SET_USER_TYPE`,
    SET_USER: `${namespace}SET_USER`
}

export const userTypes = {
    STUDENT : "STUDENT",
    BUSINESS : "BUSINESS"
}